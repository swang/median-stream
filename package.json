{
  "name": "median-stream",
  "version": "1.0.0",
  "description": "median-stream is a stream that takes an input of integers and pipes out the median of those integers",
  "main": "index.js",
  "scripts": {
    "build": "babel --presets es2015 src -d lib"
  },
  "author": "Shuan Wang",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-preset-es2015": "^6.5.0"
  }
}
